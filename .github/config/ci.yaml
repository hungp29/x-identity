apps:
  - name: x-identity
    description: x-identity gRPC service (authentication & authorization)
    cicd:
      workflows:
        jobs:
          build-and-unit-test:
            go-version: '1.25.0'
          test:
            a: 'v'
            